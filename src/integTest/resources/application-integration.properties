
spring.profiles.active = dev

# ssl

server.port = 8443
server.ssl.key-store = /home/cjstehno/.keystore
server.ssl.key-store-password = tomcat
server.ssl.key-password = tomcat

# Logging

logging.level.org.springframework = INFO
logging.level.org.springframework.security =INFO
logging.level.com.stehno.photopile = DEBUG

# database

spring.datasource.url=jdbc:postgresql://localhost:5432/photopile_integ
spring.datasource.username=photopile
spring.datasource.password=photopile
spring.datasource.driverClassName=org.postgresql.Driver

shell.ssh.enabled=false
shell.telnet.enabled=false